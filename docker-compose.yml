services:
  kiwix:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    restart: unless-stopped
environment:
  UPDATE_INTERVAL_HOURS: "12"
  KEEP_OLD_VERSIONS: "1"
  PORT: "80"